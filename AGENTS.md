# AGENTS.md — ResDyn_Front 项目规范

本文件是 Codex 的项目级指令，**每次对话都必须严格遵守以下所有规则**。

---

## 规则 1：父子组件职责分离

不在父组件中直接读写子组件内部状态（禁止 `ref.value.xxx = ...` 穿透子组件）。

- 父组件只传 props、监听 emits

- 需要调用子组件方法时可以读写子组件内部 ref

- 尽量不要在名为index.vue文件里写子组件的代码逻辑

---

## 规则 2：代码格式化 + 注释

每次生成代码必须格式化，**每个函数/方法都要加注释**，模块级别也要有注释说明用途；代码能精简就精简，避免冗余。

- 格式化使用当前项目使用的格式化规则
- 生成后检查缩进、空行、命名
- 注释描述意图而非实现
- 每个 `function` / `const 函数` 前加 JSDoc 或单行说明

---

## 规则 3：优先复用公共函数和公共样式

生成代码前先确认同类逻辑是否已有公共实现，有则 import/使用，不重新实现。



---

## 规则 4：工作记录（周报格式）

每次任务完成后，将解决的问题追加到工作记录文件最顶部。



格式：
```
## YYYY-MM-DD（工作记录-补充N）
一、问题概述
二、处理内容
三、验证结果（含涉及文件）
```

---

## 规则 5：结束语

**每次输出的最后一句必须是："TAO，我整理好了"。**

无论任何任务，最后一条文字输出必须包含该句话。

---

## 规则 6：修改清单

每次输出都要列出本次「修改了哪些文件」以及「每个文件改了哪些地方」。

- 用可点击的 markdown 链接列出文件
- 逐文件说明新增/修改/删除的函数、逻辑或配置点
- 改动多时用表格或分条列出

---

## 规则 7：只改被要求的逻辑，不动其他代码

每次修改只改 TAO 明确要求的部分，其他已有逻辑一律不动（不重构、不格式化、不顺手清理、不添加未要求的功能）。

- 收到任务后先圈定最小改动范围，只对目标代码行出手
- 收到任务后请用简短文字复述你理解的需求，确认理解无误后我们再开始代码编写
- 如果发现旁边有"顺手能改"的问题，提出来但不自行修改，等 TAO 确认后再动

---


## 规则 8：思考过程使用中文

思考过程（thinking）的输出也尽可能用中文显示，与对话回复保持语言一致。

---

